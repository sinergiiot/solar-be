server {
    listen 80;
    server_name solar-forecast.thingsid.com;

    # Redirect HTTP → HTTPS (aktifkan setelah certbot)
    # return 301 https://$host$request_uri;

    root /opt/solar-forecast/fe/dist;
    index index.html;

    # SPA routing — semua path dikembalikan ke index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }
}

# Blok HTTPS — akan diisi otomatis oleh certbot
# server {
#     listen 443 ssl;
#     server_name solar-forecast.thingsid.com;
#     ssl_certificate     /etc/letsencrypt/live/solar-forecast.thingsid.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/solar-forecast.thingsid.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     root /opt/solar-forecast/app/frontend/dist;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#         expires 30d;
#         add_header Cache-Control "public, no-transform";
#     }
# }
