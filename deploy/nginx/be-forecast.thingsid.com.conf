server {
    listen 80;
    server_name be-forecast.thingsid.com;

    # Redirect HTTP → HTTPS (aktifkan setelah certbot)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}

# Blok HTTPS — akan diisi otomatis oleh certbot
# server {
#     listen 443 ssl;
#     server_name be-forecast.thingsid.com;
#     ssl_certificate     /etc/letsencrypt/live/be-forecast.thingsid.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/be-forecast.thingsid.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 60s;
#     }
# }
